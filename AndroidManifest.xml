<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.android.keychain"
          android:sharedUserId="android.uid.system"
          >
    <application android:label="@string/app_name"
            android:allowBackup="false" >
        <service android:name="com.android.keychain.KeyChainService">
            <intent-filter>
                <action android:name="android.security.IKeyChainService"/>
            </intent-filter>
        </service>
        <service android:name="com.android.keychain.CryptOracleService">
            <intent-filter>
                <action android:name="android.security.ICryptOracleService"/>
            </intent-filter>
        </service>
        <activity android:name="com.android.keychain.KeyChainActivity"
                  android:theme="@style/Transparent"
                  android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="com.android.keychain.CHOOSER"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <receiver android:name=".KeyChainBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        <activity
            android:theme="@android:style/Theme.Holo.Light"
            android:name=".manage.KeyManagement"
            android:label="@string/manageapp_label" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".manage.ManageContacts" android:label="@string/manage_contacts" android:theme="@android:style/Theme.Holo.Light" />
        <activity android:name=".manage.ContactDetails" android:theme="@android:style/Theme.Holo.Light" />
  </application>
</manifest>
